# This repo IS the packwiz client pack (metadata only). Keep non-pack files
# out of the generated index so players never download them into their game.
README.md
.gitignore
.gitattributes
.packwizignore
*.mrpack
# Voxy renderer is All-Rights-Reserved — may be staged here locally for the
# instance, but must never be indexed or distributed.
mods/voxy-*.jar
*.jar
